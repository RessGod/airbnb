{% extends "base.html.twig" %}

{% block title %} {{ ad.title }} {% endblock %}

{% block body %}
    <div class="ad-title" style="background-image: url({{ ad.coverImage }})">
        <div class="container">
            <h1>{{ ad.title }}</h1>
            <p>{{ ad.introduction }}</p>
            <p class="h2">
                <strong>{{ ad.rooms }} chambres</strong> pour <strong>{{ ad.price }} &euro;</strong>
                par nuits
            </p>

            <a href="#" class="btn btn-primary btn-lg">Reserver!</a>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-md-8 mb-3">
                {{ ad.content | raw }}
                <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                    <ol class="carousel-indicators">
                        {# Afficher les indicateurs par image #}
                        {% for image in ad.images %}
                            <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" {% if loop.first %} class="active" {% endif %}></li>
                        {% endfor %}  
                    </ol>
                    <div class="carousel-inner">
                    {# Création de la carousel et récupération de la première ittération dans loop.first #}
                        {% for image in ad.images %}
                            <div class="carousel-item {% if loop.first %} active {% endif %}">
                                <img src="{{ image.url }}" alt="First Slide" class="d-block w-100">
                                <div class="carousel-caption d-none d-md-block">
                                    <h5>{{ ad.title }}</h5>
                                    <p>{{ image.caption }}</p>
                                </div>
                            </div>
                        {% endfor %} 
                    <a href="#carouselExampleIndicators" class="carousel-control-prev" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Précedent</span>
                    </a>
                    <a href="#carouselExampleIndicators" class="carousel-control-next" role="button" data-slide="next">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Prochain</span>
                    </a>
                </div>
            </div>
            <div class="col-md-10">
                <div class="row mb-3 align-items-center">
                    <div class="col-md-4">
                        <img src="/img/avatar.PNG" alt="Avatar" style="height: 64%; width: 64%;">
                    </div>
                    <div class="col-md-4">
                        <h3>Riba Sergino</h3>
                        <span class="badge badge-primary">3 Annonces</span>
                    </div>
                </div>
                <p>Lorem ipsum dolor sit amet consectetur, adipisicing elit. Ipsam nam ipsa veniam suscipit hic dignissimos ex quibusdam veritatis. Ab, doloribus?</p>
                <p>Quidem perspiciatis provident adipisci, fuga praesentium asperiores culpa laudantium expedita aspernatur. Doloremque quasi sit architecto, recusandae ratione voluptatum corrupti numquam.</p>
                <p>Ducimus cumque autem laborum repudiandae illo error distinctio vitae praesentium recusandae. Deserunt ea delectus libero mollitia? In quaerat laboriosam saepe.</p>
            </div>
        </div>
    </div>
{% endblock %}