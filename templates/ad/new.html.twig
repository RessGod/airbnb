{% extends "base.html.twig" %}

{% block title 'Créer une nouvelle annonce' %}

{% block body %}
    <div class="container">
        <h1>Création d'une nouvelle annonce</h1>
            {{ form_start(form) }}

                {{ form_widget(form) }}
                
                <button type="submit" class="btn btn-primary btn-lg">Créer la nouvelle annonce</button>
            {{ form_end(form) }}
            
            {# {{form(form)}} #}
    </div>
{% endblock %}